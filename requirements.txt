fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-dotenv>=1.0.1
gitpython>=3.1.43
chromadb>=0.5.18
sentence-transformers>=3.1.1
langchain-text-splitters>=0.3.0
crewai>=0.80.0
crewai-tools>=0.14.0
pydantic>=2.9.2
requests>=2.32.3
streamlit>=1.39.0

# Needed LIVE for LoRA style rewriting (app/style_service.py) — this is
# why the app now deploys to Hugging Face Spaces (16GB RAM free tier)
# instead of Render (512MB free tier, too small for these).
torch>=2.4.1
transformers>=4.45.2
peft>=0.13.2
huggingface_hub>=0.25.0
